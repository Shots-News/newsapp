import 'package:flutter/cupertino.dart';

class MyErrorScreen extends StatelessWidget {
  const MyErrorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
